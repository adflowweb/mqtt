<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.adflow.pms.domain.mapper.SummaryMapper">
<!-- cache eviction="LRU" flushInterval="6000" size="100" readOnly="true" -->

	<resultMap id="summaryResult" type="java.util.Map">
		<result property="userId" column="user_id" />
		<result property="status" column="status" javaType="int"/>
		<result property="isReservation" column="is_reservation" />
		<result property="msgCnt" column="msg_cnt" />
		<result property="pmaAckCnt" column="pma_ack_cnt" />
		<result property="appAckCnt" column="app_ack_cnt" />
	</resultMap>

	<select id="getMonthSummary" parameterType="kr.co.adflow.pms.domain.MsgParams" resultMap="summaryResult">
		SELECT a.issue_id AS user_id
		, a.status
		, a.is_reservation
		, COUNT(a.msg_id) as msg_cnt
        , COUNT(b.ack_type) AS pma_ack_cnt
        , COUNT(d.ack_type) AS app_ack_cnt
		FROM
		pms.message${keyMon} a LEFT OUTER JOIN pms.ack${keyMon} b ON a.msg_id = b.msg_id AND b.ack_type = 'pma',
        pms.message${keyMon} c LEFT OUTER JOIN pms.ack${keyMon} d ON c.msg_id = d.msg_id AND d.ack_type = 'app'		
		WHERE
		<if test="msgType != null and msgType != 0">
		AND a.msg_type = #{msgType}
		</if>
		<if test="dateStart != null ">
		AND a.update_time <![CDATA[>]]>  #{dateStart}
		</if>
		<if test="dateEnd != null ">
		AND a.update_time <![CDATA[<]]> #{dateEnd}
		</if>	
		<if test="issueId != null and issueId != ''">
		AND a.issue_id = #{issueId}
		</if>
        GROUP BY user_id, status, is_reservation
        ORDER BY user_id, status, is_reservation
	</select>
	
	<select id="getTPS" parameterType="String" resultType="double">
		SELECT COUNT(msg_id) / 10 AS TPS
		FROM pms.message${value}
		WHERE update_time > DATE_SUB(NOW(),INTERVAL 10 SECOND)
	</select>
	
</mapper>	