<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.adflow.pms.domain.push.mapper.PushMapper">

	<select id="getUfmi" parameterType="String" resultType="String">
		SELECT b.ufmi
		FROM push.token a, push.user b 
		WHERE a.userid = b.userid
		AND a.tokenid =	#{token}
		
	</select>
	
	<select id="getToken" parameterType="String" resultType="String">
		SELECT a.tokenid
		FROM push.token a, push.user b 
		WHERE a.userid = b.userid
		AND b.ufmi = #{token}
	</select>

</mapper>	