<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.adflow.push.mapper.UserMapper">

	<select id="get" resultType="kr.co.adflow.push.domain.User">
		SELECT * FROM user WHERE userid =
		#{userID}
	</select>

	<insert id="post" parameterType="kr.co.adflow.push.domain.User">
		INSERT INTO user values(
		#{userID}, #{name}, #{title}, #{dept}, #{email}, #{phone}
		)
	</insert>

	<delete id="delete" parameterType="kr.co.adflow.push.domain.User">
		DELETE from user
		WHERE
		userid=#{userID}
	</delete>
</mapper>	
	
	