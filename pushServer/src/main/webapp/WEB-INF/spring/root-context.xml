<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/util 
    http://www.springframework.org/schema/util/spring-util-3.0.xsd">


	<!-- <util:properties id="code" location="file:${user.home}/config_#{systemProperties['env']}.properties" 
		/> -->
	<util:properties id="pcs" location="classpath:config.properties" />

	<!-- Root Context: defines shared resources visible to all other web components -->

	<!-- <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean"> 
		<property name="dataSource" ref="dataSource"> <property name="configLocation" 
		value="classpath:mybatis-config.xml"> </property> </property> </bean> <bean 
		id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate"> <constructor-arg 
		ref="sqlSessionFactory"></constructor-arg> </bean> -->

	<bean id="dataSource" class="org.springframework.jndi.JndiObjectFactoryBean"
		scope="singleton">
		<property name="jndiName">
			<value>java:comp/env/jdbc/push</value>
		</property>
		<property name="resourceRef" value="true" />
	</bean>

	<!-- 트랜잭션 -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>


	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<!-- 아래부분은 매핑할 xml파일이 있는 패키지경로를 설정한다. -->
		<property name="typeAliasesPackage" value="kr.co.adflow.push.mapper" />
		<property name="mapperLocations" value="classpath:kr/co/adflow/push/mapper/*.xml" />
	</bean>


	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg name="sqlSessionFactory" ref="sqlSessionFactory" />
	</bean>


	<!-- jmsConnectionFactory -->
	<bean id="jmsFactory" class="com.ibm.mq.jms.MQTopicConnectionFactory">
		<!-- <property name="hostName" value="#{pcs['mq.jms.hostname']}" /> -->
		<!-- <property name="port" value="#{pcs['mq.jms.port']}" /> -->
		<property name="connectionNameList"
			value="#{pcs['mq.jms.hostname1']},#{pcs['mq.jms.hostname5']},#{pcs['mq.jms.hostname2']},#{pcs['mq.jms.hostname6']},#{pcs['mq.jms.hostname3']},#{pcs['mq.jms.hostname7']},#{pcs['mq.jms.hostname4']},#{pcs['mq.jms.hostname8']}," />
		<property name="queueManager" value="*" />
		<property name="transportType" value="1" />
		<property name="channel" value="#{pcs['mq.jms.channel']}" />
	</bean>

	<bean id="userCredentialsConnectionFactory"
		class="org.springframework.jms.connection.UserCredentialsConnectionFactoryAdapter">
		<property name="targetConnectionFactory" ref="jmsFactory" />
		<property name="username" value="#{pcs['mq.jms.id']}" />
		<property name="password" value="#{pcs['mq.jms.password']}" />
	</bean>

	<!-- springConnectionFactory -->
	<bean id="springConnectionFactory"
		class="org.springframework.jms.connection.CachingConnectionFactory">
		<property name="targetConnectionFactory" ref="userCredentialsConnectionFactory" />
		<property name="sessionCacheSize" value="5" />

	</bean>

	<!-- dynamicDestinationResolver -->
	<bean id="jmsDestinationResolver"
		class="org.springframework.jms.support.destination.DynamicDestinationResolver">
	</bean>

	<!-- jmsTemplate -->
	<bean id="jmsTemplate" class="org.springframework.jms.core.JmsTemplate">
		<property name="connectionFactory" ref="springConnectionFactory" />
		<property name="destinationResolver">
			<ref bean="jmsDestinationResolver" />
		</property>
		<property name="pubSubDomain">
			<value>true</value>
		</property>
	</bean>
</beans>
